# server_config.yaml

# gNMIサーバーがリッスンするポート
port: 57400

# SNMPターゲット設定 (オンボックスなら localhost)
snmp_target: "127.0.0.1"
community: "public"

# インターフェース自動検知の間隔とOID
refresh_interval_sec: 60
if_name_oid: ".1.3.6.1.2.1.2.2.1.2"

# キャッシュの更新間隔
cache_interval_sec: 10
